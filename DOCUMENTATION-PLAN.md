# 文档补全与源码核对大纲

本文是本轮维护的工作记录，不作为 GitBook 正文导航。面向读者的阅读顺序由 SUMMARY.md 提供。

## 核对基线

- 文档仓库开始时工作区干净。
- framework：ff520e61；以本地源码、项目文件、清单和测试核对，不把 README 的介绍等同于已实现行为。
- hosting：064f7cb；tools：a0a9cc5。核对启动入口、项目属性、部署清单和命令参数。
- 阅读根目录 AGENTS.md、GitBook skill 及源码目录就近的 AGENTS.md、SKILL.md、README。
- 保留已有页面路径和主体结构；补齐缺少的用法、背景、边界与排障，避免统一重排已有充实页面。

## 执行顺序与文章大纲

1. 架构与基础概念：框架分层、依赖方向、宿主与插件、模块与服务容器、提供者、配置与部署的区别；增加概念专页及术语交叉链接。
2. 快速开始：环境与版本选择、编译引用和运行部署、最小终端宿主、首个业务插件、启动验证与调试。
3. 插件框架：应用生命周期、清单与加载、构件与服务、模块解析、配置匹配与扩展点；核对示例表达式。
4. 数据引擎：对象关系与数据模式、映射与连接、同步及异步查询、导航与写入、事务、数据服务、驱动边界与排障。
5. Web：控制器插件、请求绑定和数据服务接口、认证授权、OpenAPI、gRPC、HTTP 验证路径。
6. 安全：身份与凭证、认证和授权、角色权限、验证码、Web 接入和边界。
7. 诊断：日志/指标/链路、采集与导出、OTLP 客户端和接收服务、协议边界与排障。
8. 智能化及遗漏模块：模型/助手/会话、流式调用；机器学习、网络通讯、硬件采集、报表各自的契约、实现和使用范围。
9. 消息：生产订阅与确认、Kafka/RabbitMQ/MQTT/ZeroMQ 差异、可靠存储与幂等；同步旧样例路径和失效结论。
10. 自动升级：包生产、清单、发现下载、部署执行与 Web 服务，明确各阶段职责和失败边界。
11. 外部扩展：缓存/分布式协作、任务调度/弹性、脚本/模板、表格/报表、云服务/微信、OPC、桌面升级；以公共契约组织消费路径。
12. 宿主与工具：终端/后台/Web、部署和容器；部署器、安装打包器、升级打包器、正则工具的前置条件、参数与验证。
13. 参考及核心同步：包索引、部署格式、选项格式、术语、常见问题；核对受本轮变化影响的核心 API 文档。
14. 校验：导航完整性、相对链接及锚点、frontmatter、GitBook 区块、代码标题与语言、源码链接、换行符、差异检查；记录无法执行的运行验证。

## 每篇文章的完成要求

- 解释解决什么问题、何时使用、必要背景与前置条件。
- 给出源码支持的配置和调用路径；片段清楚标明依赖及上下文。
- 说明可观察结果、失败排查和生命周期/实现限制。
- 相关概念链接到正文专题，源码定位链接到所属仓库。
- 不以篇幅或目录项数量代替完整性；不把外部服务集成宣称为离线验证通过。

## 完成情况

本轮已按大纲顺序完成文档补写与相关源码核对。新增 27 篇读者页面，更新 49 篇已有页面，并同步 SUMMARY.md；导航由 120 项扩展为 147 个不重复页面。另保留本文作为维护记录，总计修改或新增 78 个 Markdown 文件。

| 大纲阶段 | 交付内容与阅读入口 |
| --- | --- |
| 1. 架构与概念 | 重写架构与设计理念，新增[基础概念](overview/concepts.md)，解释编译依赖、运行装配和模块边界 |
| 2. 快速开始 | 核实 SDK/引用条件，补齐最小终端宿主、部署清单、[业务插件](get-started/first-business-plugin.md)及排障 |
| 3. 插件与服务 | 补充应用模型、构件表达式、宿主扫描，新增[服务定位与所有权](framework/core/services/locating.md) |
| 4. 数据引擎 | 增加背景、首次查询、数据服务与事务专页；核对映射、模式、读写分离、异步调用和驱动差异 |
| 5. Web | 新增控制器部署、[数据服务接口](framework/web/data-services.md)、OpenAPI/gRPC 专页 |
| 6. 安全 | 增加认证授权与[验证码](framework/security/captcha.md)专页，说明凭证、角色、权限和确认令牌边界 |
| 7. 诊断 | 补充日志/指标/链路背景及 [OTLP 接入](framework/diagnostics/otlp.md)，区分导出端和接收服务 |
| 8. 智能化与遗漏模块 | 增加会话生命周期与流式响应，以及机器学习、网络、硬件、报表专页，标明已实现与未完成范围 |
| 9. 消息 | 核对四种队列实现，新增概念和[可靠投递与存储](framework/messaging/reliability.md)，修正异步订阅示例 |
| 10. 自动升级 | 补充包生产、发现、部署、重启与[故障恢复](framework/upgrading/workflow.md)，明确完整更新的清理行为 |
| 11. 外部扩展 | 按能力新增缓存、脚本、调度、文档、云服务、设备与桌面更新六篇文章，覆盖现有 17 个扩展目录 |
| 12. 宿主与工具 | 更新全部宿主和工具页面，核对项目构建、入口名、参数、打包与部署操作的实际效果 |
| 13. 参考与核心同步 | 更新包索引、格式、术语、FAQ、案例，并同步受影响的 Core 服务、消息和事务正文 |
| 14. 校验 | 完成导航、相对路径、修改页面锚点、格式、示例编译、XML/XSD 与差异检查；范围见下文 |

## 关键核对结果

| 主题 | 本轮修正或补充 |
| --- | --- |
| 插件配置 | 区分宿主点号后缀和插件连字符后缀；配置文件跟随清单文件主名；说明同一插件内覆盖与跨插件顺序边界 |
| 模块与服务 | 区分模块容器、请求作用域、名称查找和具名提供者；不把服务表达式中的模块名误当提供者名 |
| 数据访问 | 入门通过通用具名提供者取得访问器；指出数据服务默认属性解析与当前注册契约的差异，示例显式注入访问器 |
| 连接与命令 | 读写连接使用基础名及井号后缀；只读命令显式声明 mutability；外部 SQL 推荐使用限定命令名 |
| 数据模式 | 修正导航数量限制和排序语法，说明计算属性需显式选择及级联操作边界 |
| 消息处理 | Message.Data 为字节数组；异步处理使用处理器契约，避免同步委托造成 async void |
| 投递与存储 | 不把发布成功等同业务完成；说明 Kafka 自动提交配置、MQTT 当前发布路径、ZeroMQ 两种通道及存储分区 |
| 事务与取消 | 修正异步事务完成和释放说明；不把收到取消或统一事务接口理解为数据库已回滚 |
| 升级与打包 | 完整部署可能先清理目录且无自动原子回滚；checksum 会更新清单；包检查不能只看进程退出码 |
| 具体实现边界 | 记录 Scriban 空变量字典、机器学习管线、报表未完成路径、InfluxDB 3 事务限制等实际条件 |

## 源码仓库中的差异与限制

以下内容已在对应中文页面中明确说明。本次修改范围为 documentation.zh，没有修改兄弟源码仓库，也没有将源码修复混入文档任务。

- Data 的 XSD 与映射加载器对复合属性 immutable、命令 mutability 的缺省值不一致；中文示例显式声明它们。
- Data 当前注册通用具名提供者，而 Core 数据服务默认属性仍尝试旧的数据访问提供者契约；示例通过通用提供者设置 DataAccess，不能照抄旧默认解析假设。
- ZeroMQ README 描述同主题重复订阅直接复用，但公共队列基类已校验处理器、标签和选项兼容性；正文以当前代码为准。
- Upgrader README 的配置示例使用 File，而随包选项文件的默认项为 Web；正文区分示例选择与实际默认值。
- Scriban 的可省略变量参数在当前实现中仍被直接访问 Count；示例传入变量字典，不把可选签名理解为运行时接受空值。
- Learning 的 Pipeline.Build 没有正确累积后续估计器；Web 训练接口仍有骨架内容。机器学习页说明限制，没有提供声称可工作的完整训练教程。
- Reporting 的部分定位/渲染入口尚未完成，默认数据加载还有分页行为；报表页及扩展页明确要求按具体实现验证。

## 验证范围与结果

- 导航：147 个页面路径均存在，无重复导航；所有新增读者页面都已入导航。
- 链接：扫描仓库 Markdown 中的内部相对路径；修改页面中的锚点也已核对，未发现失效路径或待处理锚点。GitHub main 分支源码链接的路径与本地对应源码文件核对通过；未宣称所有外部网站已在线可达。
- 结构：修改页面的 frontmatter 为简单合法键值配置；GitBook 区块、代码围栏及语言标识检查通过，主要示例带 code title。
- C#：15 组代表性片段使用本地 .NET 10 引用程序集及现有 Core/Plugins/Web 输出完成编译，零错误、零警告。包括终端入口、业务插件、数据查询/服务、Web 控制器、缓存、脚本、服务定位、硬件、调度、表格、消息处理器和事务。片段补足标准隐式 using；方法级片段置入对应类中检查。
- XML：36 个 XML 代码片段通过语法解析；其中 3 个完整映射示例进一步通过当前 Zongsoft.Data.xsd 验证。
- 文本：新增文件使用 CRLF，既有文件保留原有换行；新增代码层级使用 Tab，SUMMARY 保留空格层级；差异空白检查通过。
- 工作区：开始时无用户未提交改动；本次只更改文档，临时校验程序与编译产物在系统临时目录中。

编译与静态校验不能代替运行验证。本轮没有启动数据库、消息 Broker、云服务、模型服务或实际设备，没有执行安装、升级、IIS/systemd 重启及发布操作，也没有在线发布 GitBook。示例中的预期输出来自 API、配置和源码核对，不能视为所有外部集成已完成端到端测试。

## 后续维护方式

框架升级时，优先复核上述存在差异的契约和实现限制，再沿“清单 → 配置 → 服务注册 → 第一次调用 → 资源释放”核对受影响页面。新增能力继续放入现有栏目，并同步概念链接、参考索引与导航。源码修复后，应及时删除失效限制说明并补上相应的可执行示例。
