---
description: Zongsoft 文档中的常见问题。
icon: circle-question
---

# 常见问题

## 宿主程序里可以写业务代码吗？

不建议。宿主程序应保持为插件容器，业务能力应放在插件中，再通过部署进入宿主。

## 为什么部署后没有看到插件能力？

优先检查：

- `plugins/` 目录是否有 `*.plugin` 文件。
- 插件程序集是否已复制。
- 配置和映射文件是否在正确目录。
- 宿主启动参数中的 `host`、`site`、`environment` 是否符合预期。
- 部署命令是否使用了正确的 `framework`、`platform`、`architecture`。

## `.deploy` 和 `.option` 有什么区别？

`.deploy` 描述如何部署文件；`.option` 描述插件或模块运行时配置。

## 应该先读哪部分文档？

第一次接触建议按首页阅读路径走：概览、插件化、快速开始、插件框架、数据引擎、宿主部署。
